import styled from "@emotion/styled";
import { Paper } from "@mui/material";

const CalculatorBase = styled(Paper)({
  padding: 20,
  marginTop: 50,
  borderRadius: 30,
  backgroundColor: 'black'
});

export default CalculatorBase;
